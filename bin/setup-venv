#!/usr/bin/env bash


deactivate > /dev/null 2>&1 || true 
rm -rf venv
python3 -m venv venv

echo "Your virtual environment is now setup! :)"
echo "To activate the virtual env please execute the following: 'source venv/bin/activate'"

fasle comment
test1 branch