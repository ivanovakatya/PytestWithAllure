#!/usr/bin/env bash

python -m pip install --upgrade pip
touch requirements.txt
pip install -r requirements.txt

echo "Your project is now setup! :)"
echo "To run the API please execute the following: 'bin/run-api'"

